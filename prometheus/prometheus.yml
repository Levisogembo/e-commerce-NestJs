global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'nestjs'

    metrics_path: /api/v1/metrics

    static_configs:
      - targets:
          - ecommerce-api:3000
        labels:
          app: 'elixir-store'
